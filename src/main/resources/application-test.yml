# Test profile - Overrides for testing environment

spring:
  jpa:
    show-sql: false

# Minimal logging for tests
logging:
  level:
    root: WARN
    com.orientation.backend: INFO
    org.springframework: WARN
    org.hibernate: WARN

management:
  endpoint:
    health:
      show-details: never